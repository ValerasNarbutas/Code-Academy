import casual from "casual";

const city = casual.city;

console.log(city);
console.log(Math.random());

const parameters = `${casual.name_suffix} ${casual.first_name} ${casual.last_name}`;

console.log(parameters);